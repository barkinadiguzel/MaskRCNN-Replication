# MaskRCNN-Replication
MaskRCNN-Replication is a PyTorch implementation of Mask R-CNN, focusing on instance-level object segmentation. It provides a modular pipeline with backbone feature extraction, RPN for proposals, RoIAlign for precise alignment, and separate heads for classification, box regression, and mask prediction.
